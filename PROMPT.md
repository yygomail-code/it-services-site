# PROMPT.md — Рабочая памятка для сессий разработки

## Текущая задача (этап)
Этап 1: каркас Angular (роутинг, layout, токены, mobile-first).
Далее: контент-модели → PHP-шлюз + MySQL → формы с UTM → SEO → деплой.

## Контекст (кратко)
- Витрина ИТ-услуг одного исполнителя, две воронки: клиентская и партнёрская.
- Фронт: Angular (standalone, mobile-first), сборка с prerender → чистый HTML.
- Бэк: PHP 8.5.6 — один шлюз `api/lead.php` (Яндекс SMTP + MySQL), без мессенджеров.
- Хостинг: виртуальный сервер PHP + MySQL + статика, без Node.

## Обязательные правила
- Не менять контент/модели без согласования с пользователем.
- Заглушки для имени, телефона, фото, кейсов — заполняются позже.
- Письмо при тестах локально НЕ отправлять (`APP_ENV=dev`).
- Секреты (SMTP-пароль, ключи) — только в окружении/конфиге, не в коде и не в git.
- UTM-метки из URL автоматически подставлять в скрытые поля форм.

## Полезные команды
- Сборка (prerender): `ng build`
- Локальный dev-сервер: `ng serve`
- Git: инициализирован, ветка `main`

## Статус
- [ ] Этап 1 — каркас Angular
- [ ] Этап 2 — контент-модели (услуги, кейсы, партнёры)
- [ ] Этап 3 — PHP-шлюз + MySQL (локально)
- [ ] Этап 4 — формы с UTM + валидация
- [ ] Этап 5 — SEO (meta, JSON-LD, sitemap, robots)
- [ ] Этап 6 — prerender + деплой

## Полезные ссылки / файлы
- CONTEXT.md — полное описание проекта, стека и ограничений
- HANDOFF.md — журнал передачи сессий
