-- Удаление шаблонов страниц: тип страницы больше не используется.
-- Публичный адрес определяется слагом (home -> /, остальные -> /<slug> или /pages/<slug>).
-- Если колонки уже нет — пропустите этот файл.

ALTER TABLE pages DROP COLUMN template;
