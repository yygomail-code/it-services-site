-- ---------------------------------------------------------------------
-- Тип группы каталога: для каких разделов показывать группу.
-- kind = NULL     — для всех (услуг и товаров);
-- kind = 'service' — только для услуг;
-- kind = 'product' — только для товаров.
-- Если колонка уже есть — пропустите этот файл.
-- ---------------------------------------------------------------------
SET NAMES utf8mb4;

ALTER TABLE service_categories ADD COLUMN kind VARCHAR(16) DEFAULT NULL AFTER parent_id;
