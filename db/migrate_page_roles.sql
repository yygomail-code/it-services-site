-- Доступ к страницам по ролям (как у ссылок меню/футера).
-- roles — JSON-массив кодов ролей: NULL — доступна всем, [] — скрыта для всех,
-- список кодов — доступна этим ролям (guest — неавторизованные).
-- Если колонка уже есть — пропустите этот файл.

ALTER TABLE pages ADD COLUMN roles TEXT DEFAULT NULL AFTER status;
